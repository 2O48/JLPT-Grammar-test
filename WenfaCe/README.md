# 文法册

原生 SwiftUI 应用，最低支持 iOS 17 和 macOS 14。题库从仓库根目录的 `data.js` 转换并内置为 `WenfaCe/Resources/Grammar.json`。

## 打开与首次配置

1. 用 Xcode 打开 `WenfaCe.xcodeproj`。
2. 在 target 的 Signing & Capabilities 中选择开发团队。
3. 选择 iPhone、iPad、Mac 或模拟器运行。

此分支完全离线：练习记录、收藏和设置仅保存在当前设备。使用设置页的导入导出功能可手动迁移数据。
